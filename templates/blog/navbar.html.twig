{% block body %}

<div class="bg-img">

	<nav class="navbar navbar-expand-lg navbar-light">
		<i class="far fa-paper-plane" style="color:#DDDDDD;"></i>
		<a class="navbar-brand" href="{{path('home')}}">Accueil</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav mr-auto">
				<li class="nav-item">
					<a class="nav-link" href="{{path('blog')}}">Destinations<span class="sr-only">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('search')}}">Search<span class="sr-only"><i class="fa fa-search"></i></span>
					</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('blog_create')}}">
							<i class="fas fa-plus-circle"></i>
						{% endif %}
					</li>
				</a>
			</li>
		</ul>

		{% if app.user %}

			<ul class="nav navbar-nav navbar-right ml-auto">
				<a href="{{path('signOut')}}">
					<li class="nav-item">
						<i class="fas fa-sign-out-alt" style="color:#DDDDDD;"></i>
					</li>
				</a>
			</ul>

		{% else %}


			<ul class="nav navbar-nav navbar-right ml-auto">
				<li class="nav-item">
					<a data-toggle="dropdown" class="nav-link dropdown-toggle" href="#">Login</a>
					<ul class="dropdown-menu form-wrapper">
						<li>
							{{ include('/security/signIn.html.twig') }}
						</li>
					</ul>
				</li>
				<li class="nav-item">
					<a href="{{path('signUp')}}" class="btn btn-primary dropdown-toggle get-started-btn mt-1 mb-1">Sign up</a>

				</li>
			</ul>

		{% endif %}
	</div>
</nav>


</div>






{% endblock %}
